// Preview HTML preparation lives in core so every host can reuse it with the
// packages it already depends on. This module re-exports the core API for
// existing deep imports and for the standalone adapter.
export {
  preparePreviewHtml,
  rewritePreviewHtmlMetafileReferences,
} from '@openagentinternet/agent-browser-core';
